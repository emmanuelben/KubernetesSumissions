apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: image-apps-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: image-apps-svc
            port:
              number: 2346